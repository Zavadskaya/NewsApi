<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.example.user.news">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:largeHeap="true"
            android:allowBackup="true"
            android:icon="@mipmap/news"
            android:name=".commons.App"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/news"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.ArticleActivity"/>
        <activity android:name=".view.WebViewActivity">
        </activity>
        <activity android:name=".view.MainActivity">
        </activity>
        <activity android:name=".view.SearchActivity">

        </activity>
        <activity android:name=".view.SourceActivity">
        </activity>
        <activity android:name=".view.EverythingActivity">
        </activity>
    </application>

</manifest>